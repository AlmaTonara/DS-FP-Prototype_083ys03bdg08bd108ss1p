TestForFP = Main file with performance tests
Entity = Simple entity class to represent any type of enemies in a game
PositionRandomizer = Just return a list of coords for testing
OneD = Linear data structure which store entities raw
TwoD = Spatially indexed data structure which stores entities in a grid
HashGrid = Best, because it is spatially indexed and spatially partitioned
